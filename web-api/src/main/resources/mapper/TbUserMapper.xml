<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kimi.my.shop.web.api.dao.TbUserDao">

	<!-- sql片段 -->
	<sql id="tbUserColumns">
		a.id,
        a.username,
        a.password,
        a.phone,
        a.email,
        a.created,
        a.updated
	</sql>

	<insert id="register" parameterType="TbUser">
		insert into tb_user(username, password, email)
		values(#{username}, #{password}, #{email})
	</insert>

   <select id="login" resultType="TbUser">
	   SELECT
	   <include refid="tbUserColumns"></include>
	   FROM
		tb_user AS a
	   WHERE email = #{username}
   </select>
	<select id="isExist" resultType="TbUser">
		SELECT
		<include refid="tbUserColumns"></include>
		FROM
		tb_user AS a
		WHERE email = #{email}
	</select>
	<update id="saveUserInfo">
		update tb_user
		set  username = #{username},
			password = #{password},
			phone = #{phone},
			email = #{email}
		where id = #{id}
	</update>
</mapper>